{
  "name": "curlplan-sync",
  "private": true,
  "type": "module",
  "description": "CurlPlan unified-sync Worker (Cloudflare Workers + D1 + Clerk JWT).",
  "scripts": {
    "typecheck": "tsc --noEmit --allowJs --checkJs --strict false --noImplicitAny false --target ES2022 --module ESNext --moduleResolution Bundler --lib ES2022,DOM globals.d.ts src/index.js src/handler.js src/auth.js",
    "lint": "node ../scripts/lint-api.js",
    "test": "node ../scripts/verify-api.js",
    "test:migration": "node ../scripts/verify-d1-migration.mjs",
    "test:staging": "node ../scripts/verify-worker-staging.mjs",
    "test:staging:clerk": "node ../scripts/verify-clerk-worker-staging.mjs",
    "build": "CI=1 WRANGLER_SEND_METRICS=false wrangler deploy --dry-run --outdir=dist",
    "verify": "npm run typecheck && npm run lint && npm test && npm run test:migration && npm run build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute curlplan --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute curlplan --remote --file=./schema.sql",
    "db:migrate:v4": "wrangler d1 execute curlplan --file=./migrations/0002_atomic_sync_v4.sql",
    "db:migrate:v4:remote": "wrangler d1 execute curlplan --remote --file=./migrations/0002_atomic_sync_v4.sql"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "wrangler": "4.123.0"
  }
}
